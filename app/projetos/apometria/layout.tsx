import { createPageMetadata } from "../../../lib/site";
export const metadata = createPageMetadata({ title: "Apometria", description: "Informações e orientações iniciais sobre os atendimentos de Apometria realizados pela Casa Sol do Oriente.", path: "/projetos/apometria", image: "/casa-sol/hero-apometria-casa.jpg" });
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
