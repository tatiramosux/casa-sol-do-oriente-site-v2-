import { createPageMetadata } from "../../lib/site";
export const metadata = createPageMetadata({ title: "Amalá", description: "Orientações da Casa Sol para o preparo e a entrega consciente de oferendas dentro de sua prática ritualística.", path: "/amala", image: "/casa-sol/banner-amala.webp" });
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
